{
  "name": "hypestyle-launchpad",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix next-app",
    "build": "npm run build --prefix next-app",
    "vercel-build": "npm run build --prefix next-app",
    "start": "npm run start --prefix next-app",
    "lint": "npm run lint --prefix next-app"
  },
  "dependencies": {
    "next": "14.2.29",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {}
}
