# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secretos — nunca commitear (defensa: que un `git add .` no los arrastre)
.env
.env.*
!.env.example
*.env
.secrets/
**/.secrets/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.next
out
build
next-app/.next
next-app/out
next-app/node_modules

# Salidas de los tests (Playwright)
next-app/test-results/
next-app/playwright-report/
next-app/blob-report/
next-app/.playwright/

# Skill de audit SEO (SEOmator) — se instala con `npx skills add`, no se commitea
.agents/
skills-lock.json
seomator.toml
